name: chit_chat
description: "A new basic chat application to learn firebase"
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2
  font_awesome_flutter: ^10.9.0

  #firebase
  firebase_core: ^4.0.0
  firebase_app_check: ^0.4.0
  firebase_auth: ^6.0.0

  #UI state management
  get: ^4.7.2



dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  change_app_package_name: ^1.5.0


flutter:
  uses-material-design: true
  generate: true

  assets:
    - assets/images/

